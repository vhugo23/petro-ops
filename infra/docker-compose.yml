services:
  dev:
    build:
      context: ..
      dockerfile: infra/Dockerfile.dev
    container_name: petroops-ai-dev
    working_dir: /workspace
    volumes:
      - ..:/workspace
    env_file:
      - ../.env.example
    command: /bin/bash
    tty: true
    stdin_open: true